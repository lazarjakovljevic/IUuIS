<Application x:Class="NetworkService.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NetworkService"
             xmlns:viewModel="clr-namespace:NetworkService.ViewModel"
             xmlns:views="clr-namespace:NetworkService.Views"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Global Color Palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2E3440"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#3B4252"/>
        <SolidColorBrush x:Key="AccentColor" Color="#5E81AC"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#ECEFF4"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#2E3440"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#4C566A"/>

        <!-- DataTemplates for automatic View-ViewModel binding -->
        <DataTemplate DataType="{x:Type viewModel:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:NetworkEntitiesViewModel}">
            <views:NetworkEntitiesView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:NetworkDisplayViewModel}">
            <!-- Will be implemented later -->
            <Border Background="{StaticResource BackgroundColor}" Padding="20">
                <TextBlock Text="Network Display View - Coming Soon!" 
                          FontSize="18" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"
                          Foreground="{StaticResource TextColor}"/>
            </Border>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:MeasurementGraphViewModel}">
            <!-- Will be implemented later -->
            <Border Background="{StaticResource BackgroundColor}" Padding="20">
                <TextBlock Text="Measurement Graph View - Coming Soon!" 
                          FontSize="18" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"
                          Foreground="{StaticResource TextColor}"/>
            </Border>
        </DataTemplate>

    </Application.Resources>
</Application>